import {describe,expect,it} from "vitest";import {promptPreview,validateCodexUrl,validateConversation,type CodexConversation} from "./codexConversation";
const value:CodexConversation={id:"test",name:"Essai",status:"draft",prompt:"contenu factice",conversationUrl:null,projectId:null,createdAt:"2026-01-01T00:00:00Z",updatedAt:"2026-01-01T00:00:00Z",launchedAt:null};
describe("Codex conversation",()=>{it("valide le modèle",()=>expect(validateConversation(value)).toEqual([]));it("extrait exactement cinq lignes non vides",()=>expect(promptPreview(" un\n\n deux\n trois\nquatre\ncinq\nsix")).toBe("un\ndeux\ntrois\nquatre\ncinq"));it("valide seulement chatgpt.com en HTTPS",()=>{expect(validateCodexUrl("https://chatgpt.com/codex/example")).toContain("chatgpt.com");expect(validateCodexUrl("javascript:alert(1)")).toBeNull();expect(validateCodexUrl("https://evil.example")).toBeNull()})});
