import { readFileSync } from 'node:fs';import { describe,expect,it } from 'vitest';
describe('PWA',()=>{it('déclare un manifeste borné et installable',()=>{const m=JSON.parse(readFileSync('public/manifest.webmanifest','utf8'));expect(m).toMatchObject({start_url:'./',scope:'./',display:'standalone'});expect(m.icons.length).toBeGreaterThanOrEqual(2)});it('borne le service worker au scope et nettoie les caches',()=>{const sw=readFileSync('public/sw.js','utf8');expect(sw).toContain("startsWith(APP_SCOPE)");expect(sw).toContain('caches.delete');expect(sw).toContain("mode==='navigate'")});});
