import { describe,expect,it } from 'vitest';import { createProject,emptyProject } from '../../src/domain/project';import { buildExport,parseImport,serializeExport } from '../../src/domain/transfer';
const project=(name:string,active=false)=>createProject({...emptyProject(),name,isActive:active},'2026-01-01T00:00:00.000Z');
describe('import/export',()=>{it('effectue un aller-retour fidèle, archives incluses',()=>{const a=project('A',true),b={...project('B'),status:'archived' as const};expect(parseImport(serializeExport([a,b]))).toEqual([a,b])});it('refuse doublons, actifs multiples et version inconnue',()=>{const a=project('A',true),b={...project('B',true)};expect(()=>buildExport([a,b])).toThrow(/Plusieurs/);const x=JSON.parse(serializeExport([a]));x.projects.push(a);expect(()=>parseImport(JSON.stringify(x))).toThrow(/dupliqués/);x.formatVersion=9;expect(()=>parseImport(JSON.stringify(x))).toThrow(/Version/)});it('refuse un import déraisonnablement volumineux',()=>expect(()=>parseImport(' '.repeat(2_000_001))).toThrow(/volumineux/));});
