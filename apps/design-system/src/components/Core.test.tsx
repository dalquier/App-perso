import {expect,test} from 'vitest';import {render,screen} from '@testing-library/react';import {Button,Chip,Field,IconButton} from './Core';test('loading retains label and reports busy',()=>{render(<Button loading>Enregistrer</Button>);expect(screen.getByRole('button',{name:/Enregistrer/})).toHaveAttribute('aria-busy','true')});test('icon button has label',()=>{render(<IconButton label="Favori">☆</IconButton>);expect(screen.getByRole('button',{name:'Favori'})).toBeVisible()});test('error is associated',()=>{render(<Field id="postal" label="Code postal" status="error" message="Saisissez 5 chiffres"/>);expect(screen.getByLabelText('Code postal')).toHaveAccessibleDescription('⚠ Saisissez 5 chiffres')});test('chip exposes pressed state',()=>{render(<Chip selected>Actif</Chip>);expect(screen.getByRole('button',{name:/Actif/})).toHaveAttribute('aria-pressed','true')});
