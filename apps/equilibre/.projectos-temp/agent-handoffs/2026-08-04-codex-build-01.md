# Handoff temporaire — Équilibre BUILD-01

- **Objectif** : livrer la PWA minimale locale et testable sur iPhone liée à l'issue #17.
- **État GitHub vérifié** : dépôt public `dalquier/App-perso` ; `main` et la branche cible pointaient sur `37a1393` avant travaux ; PR de reprise #16 fusionnée ; issue #17 ouverte ; `apps/equilibre/` absent.
- **Précontrôle** : lecture distante disponible via Git/HTTPS et API GitHub ; écriture terminal indisponible faute de credentials ; publication attendue via l'intégration native `make_pr`. Mode visé : `github-natif`.
- **Hypothèses** : PWA vanilla/Vite, stockage `localStorage` version 1, simulateur strictement local, garde-fou lexical déterministe.
- **Actions** : inventaire en lecture seule des cinq ensembles historiques ; création du shell PWA, accueil, conversation, séance en quatre étapes, reprise, réglages, suppression, thèmes, service worker, icônes, tests et documentation Replit/iPhone.
- **Fichiers** : tous les ajouts sont sous `apps/equilibre/` ; consulter `git diff --stat origin/main`.
- **Contrôles** : `npm ci` (101 paquets, audit sans vulnérabilité), `npm test` (14 tests passants), `npm run build` (8 modules, succès), contrôle HTTP local prévu avant publication.
- **Limites/risques** : test réel iPhone/Replit non disponible dans ce conteneur ; filtre sensible non exhaustif ; `localStorage` non chiffré ; simulateur volontairement simple ; cache hors-ligne à confirmer sur Safari ; tentative de capture automatisée bloquée par les dépendances système/proxy apt.
- **Prochaine action** : vérifier la PR, tester sur Replit et iPhone avec `docs/IPHONE_TEST.md`, puis supprimer ce fichier temporaire avant fusion.
- **Branche** : `equilibre/build-01-minimal-pwa`.
- **Commit/PR** : à compléter après commit et publication native.
- **Retour arrière** : revert du commit BUILD-01 et effacement de la clé navigateur `equilibre.local.v1`.
