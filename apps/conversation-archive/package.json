{
  "name": "@projectos/conversation-archive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Resilient staging and verified retention for complete conversation archives.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js",
    "mcp:stdio": "node src/mcp-cli.js",
    "mcp:http": "node src/mcp-http.js"
  },
  "engines": {
    "node": ">=20"
  }
}
